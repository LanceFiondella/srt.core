JMModel <- function(data)
{
 newdata<-data
 n = length(data[,1])+1  
 interfail[1,2] = data[1,2]
 interfail[1,1] = data[1,1]
 for (i in 2:n){
  interfail[i,2] = data[i,2] - data[i-1,2]
  interfail[i,1] = data [i,1]
 }




  return(interfail)
}

GeoModel <- function(data)
{
 newdata <- data
 newdata[,2] <- 300 
 return(newdata)

}
GoelOkModel <- function(data)
{
 newdata <- data
 newdata[,2] <- newdata[,2] * 2
 return(newdata)

}
YamadaModel <- function(data)
{
 newdata <- data
 newdata[,2] <- newdata[,1]
 return(newdata)

}

