# Gamma model
Gamma_input <- c("FT")
Gamma_methods <- c("EM")
Gamma_params <- c("aMLE","bMLE","cMLE")
Gamma_type <- c("NHPP", "GAMMA")
Gamma_numfailsparm <- c(1)
Gamma_fullname <- c("Gamma")
Gamma_plotcolor <- c("black")
Gamma_Finite <- TRUE
