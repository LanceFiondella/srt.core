# Truncated-Max Model
TMAX_input <- c("FT")
TMAX_methods <- c("IECM")
TMAX_params <- c("aMLE","bMLE","cMLE")
TMAX_numfailsparm <- c(1)
TMAX_fullname <- c("Truncated-Max")
TMAX_plotcolor <- c("red")
TMAX_type <- c("FR","Exp")
TMAX_Finite <- FALSE
TMAX_Results<- data.frame("format"="xlsx","fileName"="model_data") # Sheet name is generated in test script, based on method.
# TMAX_Results<- data.frame("format"="xlsx","fileName"="model_data","sheet"="TMAX_Results")
