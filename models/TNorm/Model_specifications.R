# TNorm model
TNorm_input <- c("FT")
TNorm_methods <- c("EM")
TNorm_params <- c("aMLE","bMLE","cMLE")
TNorm_type <- c("NHPP", "Truncated Normal")
TNorm_numfailsparm <- c(1)
TNorm_fullname <- c("Truncated Normal")
TNorm_plotcolor <- c("black")
TNorm_Finite <- TRUE
